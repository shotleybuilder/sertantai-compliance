# ==============================================================================
# FRONTEND ENVIRONMENT VARIABLES
# ==============================================================================
# Copy this file to .env and update values for local development
# DO NOT commit .env file (it's in .gitignore)
#
# IMPORTANT: All variables must be prefixed with PUBLIC_ to be exposed to browser
# See: https://kit.svelte.dev/docs/modules#$env-static-public

# ==============================================================================
# API Configuration
# ==============================================================================
# Development: Local Phoenix backend
# Production: Your backend API domain (must match backend PHX_HOST)
PUBLIC_API_URL=http://localhost:4004

# Development: Local Electric via Docker
# Production: Your ElectricSQL domain provided by infrastructure
PUBLIC_ELECTRIC_PROXY_URL=http://localhost:3003
PUBLIC_ELECTRIC_URL=http://localhost:3003

# ==============================================================================
# Environment
# ==============================================================================
# Options: development, staging, production
PUBLIC_ENV=development

# ==============================================================================
# Authentication (if using)
# ==============================================================================
# Token refresh buffer in milliseconds (1 minute)
PUBLIC_TOKEN_REFRESH_BUFFER_MS=60000

# ==============================================================================
# Feature Flags
# ==============================================================================
# Enable debug logging in browser console
PUBLIC_ENABLE_DEBUG=true

# Enable offline mode features
PUBLIC_ENABLE_OFFLINE=true

# ==============================================================================
# PRODUCTION ENVIRONMENT VARIABLES
# ==============================================================================
# These should be set in your infrastructure/deployment environment
# Build-time variables for Docker image:
#
# PUBLIC_API_URL=https://api.yourdomain.com
# PUBLIC_ELECTRIC_URL=https://electric.yourdomain.com
# PUBLIC_ELECTRIC_PROXY_URL=https://electric.yourdomain.com
# PUBLIC_ENV=production
# PUBLIC_ENABLE_DEBUG=false
# PUBLIC_ENABLE_OFFLINE=true
